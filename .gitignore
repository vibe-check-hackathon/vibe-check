# Ignore raw design-tool exports; extracted files live in stitch-export/
*.zip
# Generated pipeline artifacts
laura/pipeline/output/
laura/pipeline/inbox/
# LLM API key (24h cache written by set-key.js) — never push
laura/pipeline/.llm-key.json
# Resend / ElevenLabs keys written by set-service-key.js — never push
laura/pipeline/.service-keys.json
# Person-level link collections stay local by policy — never push
laura/opportunity-db/founder-profiles/
# Account credentials (password hashes + salts) — never push
laura/pipeline/accounts.json
# Exact founder-evaluation methodology (interview scripts, scoring rubrics) —
# competitive IP if this launches; keep local only, never push
laura/Expanded_Founder_Evaluation_Interview_Scorecard.md
laura/Founder_Conflict_Alignment_Interview_Framework.md
laura/founder_personality_interview_framework(1).md
