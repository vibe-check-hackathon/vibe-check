# Stitch export — design reference only

Exported from Google Stitch (prompt: see ../stitch-ai-prompt.md).

- `code.html` — generated investor-view mockup. Uses CDN Tailwind + Google Fonts,
  so it needs internet and is NOT production code; we mine it for layout/spacing
  ideas only. The hand-built app in `../frontend/index.html` stays the source of truth.
- `DESIGN.md` — the Material-style token sheet (dark mode only).
- `screen.png` from the export was a broken 28-byte file and was not committed.
